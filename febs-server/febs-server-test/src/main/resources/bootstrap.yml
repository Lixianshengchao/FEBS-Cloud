spring:
  application:
    name: FEBS-Server-Test
  cloud:
    nacos:
      config:
        server-addr: ${nacos.url:192.168.50.168}:8848
        group: DEFAULT_GROUP
        prefix: febs-server-test
        file-extension: yaml
      discovery:
        server-addr: ${nacos.url:192.168.50.168}:8848

logging:
  level:
    com:
      alibaba:
        cloud:
          nacos:
            client:
              NacosPropertySourceBuilder: error
